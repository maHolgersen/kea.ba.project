@model IEnumerable<KEA.BA.Project.Models.Shopping_group>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.group_size)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Store.store_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.group_size)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Store.store_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.group_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.group_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.group_ID })
        </td>
    </tr>
}

</table>
